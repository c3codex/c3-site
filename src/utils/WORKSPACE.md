# Working Copy: priceless-gallery-spine

This folder is the active build workspace.
The deployed repo remains unchanged until an intentional sync/commit step.

Sync is performed by copying selected folders into the live repo, then committing there.
