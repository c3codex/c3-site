import React from "react";
import { NavLink, Outlet } from "react-router-dom";

export default function C3Layout() {
  return (
    <main className="min-h-screen bg-obsidian">
      <div className="mx-auto max-w-6xl px-6 pt-14 pb-10">
        <p className="font-sans text-xs tracking-[0.3em] uppercase text-gold/60">
          c3 Community Partners DAO
        </p>
        <h1 className="font-serif text-4xl md:text-5xl text-stone-200 mt-3">
          The c3 Model
        </h1>
        <p className="mt-4 max-w-2xl text-stone-500">
          Connect · Contribute · Create — a civic pattern for coherent community, culture, and restoration.
        </p>

        {/* Chamber nav */}
        <div className="mt-8 flex flex-wrap gap-4">
          <C3Tab to="/c3/connect">Connect</C3Tab>
          <C3Tab to="/c3/contribute">Contribute</C3Tab>
          <C3Tab to="/c3/create">Create</C3Tab>
        </div>

        {/* Chamber content */}
        <div className="mt-10">
          <Outlet />
        </div>
      </div>
    </main>
  );
}

function C3Tab({ to, children }: { to: string; children: React.ReactNode }) {
  return (
    <NavLink
      to={to}
      className={({ isActive }) =>
        [
          "rounded-full px-4 py-2 border text-xs font-sans uppercase tracking-[0.25em] transition",
          isActive
            ? "border-gold/40 text-gold bg-black/40"
            : "border-white/10 text-stone-400 hover:text-stone-200 hover:border-white/20",
        ].join(" ")
      }
    >
      {children}
    </NavLink>
  );
}
